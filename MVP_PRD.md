# SupRight MVP PRD

> 状态：已实现，公开测试版 v0.1.0
> 日期：2026-07-23
> 平台：macOS 14 及以上（当前已在 macOS 26.5 完成最小技术验证）

## 1. 产品定义

SupRight 是一个开源的 macOS Finder 右键增强工具。它在所有 Finder 目录提供少量、高频、可预期的文件操作，减少“打开终端 / 新建空文件 / 查找路径”等重复操作。

首版以原生 Finder Sync 实现，不复制或迁移 iRightMouse 的代码、资源、名称、配置格式或行为细节。

## 2. MVP 目标

让用户在 Finder 中完成以下高频操作：

1. 在当前目录快速创建常见空文件。
2. 一键复制文件名或完整路径。
3. 默认在所有 Finder 目录中提供菜单，并按需开关菜单功能。
4. 在当前 Finder 目录快速打开 Terminal。

MVP 成功的定义：用户无需打开终端，即可在已授权目录中稳定看到 SupRight 菜单，并完成上述操作；所有新建文件均不覆盖现有文件。

## 3. 非目标

以下内容不属于 MVP：

- 剪切、粘贴、批量复制或移动文件。
- 常用目录、外部 App、模板导入与菜单拖拽排序。
- 云盘/File Provider 的专项兼容。
- 永久删除、提权、联网服务、遥测、账号体系或配置迁移。
- 当前公开测试版提供未签名、未公证的 `.app` ZIP；后续正式发布采用站外 Developer ID 签名与公证。

## 4. 目标用户与场景

| 用户 | 场景 | 需要完成的事 |
| --- | --- | --- |
| 开发者 | 项目目录中补充说明文件 | 新建 `Untitled.md` 或 `Untitled.json` |
| 普通 Mac 用户 | 整理资料、向他人发送文件位置 | 复制选中文件的名称或完整路径 |
| 首次使用者 | 希望只在自己的工作目录启用功能 | 选择一个目录并授权 SupRight 使用 |

## 5. 功能范围

### 5.1 主 App：全目录模式、权限状态与功能开关

- 默认启用“所有 Finder 目录”模式，不要求首次选择工作目录。
- 该模式在普通可访问目录中工作；受系统保护的目录需要用户在系统设置中手动授予 SupRight“完全磁盘访问”后才能可靠操作。
- 主 App 提供“前往完全磁盘访问设置”按钮；按钮只能打开系统设置，不能替用户开启权限或绕过系统确认。
- 主 App 不读取或伪造系统授权开关；它通过读取现有受保护目录进行保守的实际访问测试。测试成功则隐藏“完全磁盘访问”引导，失败或无法确认时才显示引导。
- App 提供独立开关：七种预设“新建文件”类型、一个“自定义文件”入口、复制文件名、复制完整路径、在终端中打开；默认全部开启。
- 开关变更写入 App Group；Finder 在下一次打开右键菜单时读取最新值，不要求用户重启 Finder。
- 所有新建类型关闭时隐藏“新建文件”子菜单；当前上下文没有任何启用功能时隐藏整个 `SupRight` 菜单。

约束：功能开关等本地配置通过 App Group 在主 App 与 Finder 扩展之间共享。全目录模式不以目录书签作为访问授权来源；书签保留为未来“仅限指定目录”模式的可选能力。

### 5.2 Finder 右键菜单

默认在所有 Finder 目录中显示 SupRight 菜单项。受系统保护、只读或无写权限的位置仍会在执行操作时显示清晰错误，而不会绕过 macOS 访问控制。

菜单结构：

```text
SupRight
├── 新建文件
│   ├── TXT 文件
│   ├── Markdown 文件
│   ├── JSON 文件
│   ├── RTF 文件
│   ├── Word 文件
│   ├── Excel 文件
│   ├── PowerPoint 文件
│   └── 自定义文件…
├── 复制文件名
├── 复制完整路径
└── 在终端中打开
```

显示规则：

- 右键空白处：新建文件的目标为当前 Finder 目录；复制操作不显示。
- 右键单个文件：新建文件的目标为该文件所在目录；复制操作显示。
- 右键单个文件夹：新建文件的目标为该文件夹；复制操作显示。
- 右键多个项目：仅显示复制操作；新建文件不显示。
- 右键空白处或单个目录时：显示“在终端中打开”；文件使用其所在目录；多选时不显示。
- 菜单仅显示已启用且符合当前上下文的功能，不保留空子菜单或空白分隔行。

### 5.3 新建文件

- 支持创建 UTF-8 空白 `.txt`、`.md`、`.json` 文件，以及 `.rtf`、`.docx`、`.xlsx`、`.pptx` 文件。
- 默认名称分别为 `Untitled.txt`、`Untitled.md`、`Untitled.json`、`Untitled.rtf`、`Untitled.docx`、`Untitled.xlsx`、`Untitled.pptx`。
- 若名称已存在，依次使用 `Untitled 2.txt`、`Untitled 3.txt` 等后缀，绝不覆盖原文件。
- “自定义文件…”弹出空白输入框，由用户填写完整文件名，例如 `Untitled.py`、`config.yaml` 或 `notes.md`。同名时保留后缀并自动生成 `Untitled 2.py`，绝不覆盖原文件。
- 创建后在 Finder 中选中新文件。
- 无法创建时显示清晰错误提示，不影响原文件。

Office 文件实现约束：

- RTF 使用由 SupRight 生成的最小有效 RTF 内容。
- DOCX、XLSX、PPTX 使用项目自行创建并随 App 打包的空白模板，创建时复制模板到目标目录后再做重名处理。
- 模板必须可分别被 Microsoft Word、Excel、PowerPoint 和 Apple Pages、Numbers、Keynote 正常打开；不包含任何第三方产品的素材或版权内容。
- 即使用户未安装 Office，SupRight 也可以创建文件；打开文件由用户系统中已安装的关联应用决定。

### 5.4 复制文件名与完整路径

- 单选：将文件/文件夹名称或 POSIX 完整路径写入剪贴板。
- 多选：按 Finder 当前选择顺序写入，每项一行。
- 复制不改变文件内容、位置、名称或权限。

### 5.5 在终端中打开

- 使用系统 Terminal.app 打开一个新窗口或标签页，工作目录为当前 Finder 目录。
- 右键单个目录时，工作目录为该目录；右键文件时，工作目录为文件所在目录。
- 不自动执行 Shell 命令，不读取终端历史，不修改用户的 Shell 配置。
- 若 Terminal.app 无法启动或当前目录不可访问，显示可理解的错误提示。

## 6. 交互与文案

- 产品名称统一使用 `SupRight`。
- 首版 UI 支持“跟随系统 / 简体中文 / English”三种显示语言；选择后同时作用于配置页、菜单栏与 Finder 右键菜单。
- App 图标采用原创的白底极简鼠标箭头与菜单角标标识；不使用或近似第三方产品标志。
- Finder 菜单文案：
  - `SupRight：新建 TXT 文件`
  - `SupRight：新建 Markdown 文件`
  - `SupRight：新建 JSON 文件`
  - `SupRight：新建 RTF 文件`
  - `SupRight：新建 Word 文件`
  - `SupRight：新建 Excel 文件`
  - `SupRight：新建 PowerPoint 文件`
  - `SupRight：复制文件名`
  - `SupRight：复制完整路径`
  - `SupRight：在终端中打开`
- 任何失败都给出动作结果和原因，不静默失败。

## 7. 权限与数据边界

- 默认在 Finder 的全部本地目录中提供菜单，但只在用户触发具体操作时访问对应路径；不做后台扫描、索引或联网。
- 正式发布走站外分发：主 App 不启用 App Sandbox 并使用 Developer ID 签名和 Apple 公证；Finder Sync 扩展保留系统要求的沙盒，只转交用户点击的操作给主 App 执行。
- 完全磁盘访问必须由用户在“系统设置 > 隐私与安全性”中自行开启；SupRight 不尝试自动获得、模拟点击或绕过该权限。
- 当前配置仅保存在本机 App Group 中。
- 即使已授予完全磁盘访问，POSIX 权限、ACL、SIP 或只读卷仍可阻止写入；SupRight 必须给出可理解的失败提示。

## 8. 技术约束

- 原生技术栈：Swift、SwiftUI、AppKit、Finder Sync。
- 组成：主 App + Finder Sync Extension。
- Finder 扩展只负责识别上下文、展示菜单和执行轻量操作；MVP 不引入后台任务队列。
- 支持从源码通过 Xcode 本机构建和运行；仓库采用 MIT 许可证。
- Office 模板作为应用资源随源码维护，并在自动化测试中验证 ZIP/XML 结构和可打开性。

## 9. 验收标准

### 9.1 必须通过

- 主 App 默认覆盖全部 Finder 目录，并可独立开关每个菜单功能；下一次打开 Finder 右键菜单时立即生效。
- 主 App 提供“前往完全磁盘访问设置”入口，并明确该权限需要用户手动开启。
- 重启 Finder 后，任意可浏览目录的右键菜单出现 SupRight 项。
- 在空白处、文件、文件夹和多选场景中，菜单显示符合第 5.2 节规则。
- 七种文件均能创建；重名时自动递增后缀且不覆盖原文件。
- 单选和多选时，复制的名称/路径与 Finder 选中项一致。
- 在空白处、文件和文件夹右键时，能以正确工作目录打开 Terminal.app；多选时不显示此操作。
- 对只读、系统保护或无权限目录，保留菜单但在执行时给出可理解的失败提示。
- `xcodebuild` Debug 构建成功。

### 9.2 手工测试矩阵

| 场景 | 预期结果 |
| --- | --- |
| 任意普通目录空白处右键 | 显示七个预设“新建文件”操作、“自定义文件…”和“在终端中打开” |
| 任意普通目录单个文件右键 | 显示新建与两种复制操作 |
| 任意普通目录单个文件夹右键 | 新建目标为该文件夹 |
| 任意普通目录多选右键 | 只显示复制操作 |
| 只读或系统保护目录右键 | 显示 SupRight；写入操作给出失败提示 |
| `Untitled.txt` 已存在 | 创建 `Untitled 2.txt`，原文件不变 |
| 创建 DOCX/XLSX/PPTX | 文件能被对应 Office 和 Apple iWork 应用打开 |
| 文件夹右键“在终端中打开” | Terminal.app 的工作目录为该文件夹 |
| 文件右键“在终端中打开” | Terminal.app 的工作目录为其父目录 |
| Finder 重启 | 菜单恢复可用 |

## 10. 里程碑与完成顺序

1. **MVP-0（完成）**：目录授权、Finder Sync 注册、右键创建 TXT 的最小闭环。
2. **MVP-1**：补齐 Markdown、JSON、RTF 创建、目标目录解析与重名命名测试。
3. **MVP-2**：创建和验证 Word、Excel、PowerPoint 空白模板。
4. **MVP-3**：补齐复制文件名/完整路径、多选处理和“在终端中打开”。
5. **MVP-4**：完善主 App 状态、功能开关、书签失效处理、自动化测试与 README。
6. **MVP 评审**：按第 9 节在当前 macOS 和 macOS 14/15 环境验收，再决定是否进入 v1 扩展功能。

## 11. MVP 之后的候选方向

- 常用目录与外部 App 打开。
- 自定义文件模板。
- 剪切、粘贴、复制到、移动到。
- 菜单排序与中英文界面。

这些候选项必须在 MVP 验收完成后重新评估，不默认进入首版。
