# SupRight MVP UI 设计规格

> 对应：[MVP_PRD](MVP_PRD.md)
> 状态：已实现，公开测试版 v0.1.0
> 日期：2026-07-23

## 1. 设计目标

SupRight 的界面应当像 Finder 的自然延伸：用户右键后立刻理解操作作用的位置，不需要学习复杂设置，也不因一次文件操作被打断。

设计原则：

- **少而明确**：MVP 只显示当前上下文可用的操作。
- **目标可预测**：新建和打开终端始终指向用户当前看到或右键的目录。
- **默认安全**：不覆盖文件、不执行 Shell 命令、不在未授权位置显示菜单。
- **系统一致**：使用 macOS 原生菜单、系统字体、SF Symbols 和标准对话框。

## 2. 信息架构

```text
SupRight
├── Finder 右键菜单（主要操作入口）
│   ├── 新建文件
│   ├── 复制文件名 / 路径
│   └── 在终端中打开
├── 主 App（配置与状态入口）
│   ├── 概览
    ├── 菜单功能开关
│   ├── 权限与访问
│   └── 诊断
└── 菜单栏状态项
    ├── 已启用功能数量
    ├── 打开设置
    └── 权限状态
```

MVP 不在主 App 内放置菜单排序、模板管理、常用目录或外部 App 设置；功能开关、“完全磁盘访问”引导和显示语言是配置能力。

## 3. Finder 右键菜单

### 3.1 菜单结构

```text
SupRight
├── 新建文件 ›
│   ├── 文本文档（.txt）
│   ├── Markdown 文档（.md）
│   ├── JSON 文件（.json）
│   ├── RTF 文档（.rtf）
│   ├── Word 文档（.docx）
│   ├── Excel 工作簿（.xlsx）
│   ├── PowerPoint 演示文稿（.pptx）
│   └── 自定义文件…
├── 复制文件名
├── 复制完整路径
└── 在终端中打开
```

规则：

- Finder Sync 提供一个顶层 `SupRight` 子菜单，不混入系统菜单层级。
- 只有“新建文件”使用二级菜单，避免预设格式与自定义入口撑高一级右键菜单。
- MVP 不使用分隔线，避免 Finder 子菜单出现多余留白。
- 菜单项以动作描述为主，扩展名作为辅助信息；不使用技术缩写作为唯一标签。

### 3.2 上下文显示矩阵

| Finder 上下文 | 新建文件 | 复制文件名 | 复制完整路径 | 在终端中打开 | 操作目标 |
| --- | --- | --- | --- | --- | --- |
| 已授权目录的空白处 | 显示 | 隐藏 | 隐藏 | 显示 | 当前 Finder 目录 |
| 单个文件 | 显示 | 显示 | 显示 | 显示 | 文件父目录（新建/终端）；文件本身（复制） |
| 单个文件夹 | 显示 | 显示 | 显示 | 显示 | 该文件夹（新建/终端）；文件夹本身（复制） |
| 多选文件或文件夹 | 隐藏 | 显示 | 显示 | 隐藏 | 所有选中项（复制） |
| 未授权目录 | 整个菜单隐藏 | 整个菜单隐藏 | 整个菜单隐藏 | 整个菜单隐藏 | 无 |

### 3.3 结果反馈

| 操作 | 成功反馈 | 失败反馈 |
| --- | --- | --- |
| 预设新建文件 | Finder 选中新建文件，默认不弹窗 | 标准警告框：说明无法创建的原因和目标目录 |
| 自定义文件 | 弹出输入框，填写完整文件名（如 `Untitled.py`）后创建并在 Finder 中选中；同名自动加序号 | 标准警告框：提示输入有效的文件名或无法写入目标目录 |
| 复制名称/路径 | 静默复制到剪贴板 | 标准警告框：说明无法读取选中项 |
| 在终端中打开 | 启动 Terminal.app 并进入目标目录 | 标准警告框：说明无法打开 Terminal 或目标目录 |

新建重名时使用自动递增后缀，不询问用户：`Untitled 2.txt`、`Untitled 3.txt`……。这是可预测且不破坏现有文件的默认行为。

## 4. 主 App：配置页

### 4.1 版式

窗口建议宽度 `920 pt`、高度 `620 pt`。使用类似 Codex 的浅色侧边栏和内容工作区；侧边栏用于未来功能扩展，当前启用“概览”“菜单功能”“权限与访问”“诊断”“语言”。选中态、开关和状态标签使用 `#ffacda → #0ec1ff` 渐变，内容卡保持白底。

#### 默认概览状态

```text
┌──────────────────────────────────────────────────────┐
│  侧边栏                  概览                         │
│  ● 概览                 Finder 右键增强已启用         │
│    菜单功能             10 项功能已开启               │
│    权限与访问                                         │
│    诊断                 完全磁盘访问：请在系统设置确认 │
│                         受保护目录可能无法创建文件。  │
│                         [ 前往系统设置 ]               │
└──────────────────────────────────────────────────────┘
```

#### 权限与访问页

```text
┌──────────────────────────────────────────────────────┐
│  权限与访问                                            │
│                                                       │
│  所有 Finder 目录                                      │
│  菜单默认在所有目录显示；不会扫描或读取未操作的文件。  │
│                                                       │
│  完全磁盘访问           需要在系统设置中确认           │
│  仅用于访问 macOS 受保护目录。该权限须由你手动开启。   │
│  [ 前往系统设置 ]                                      │
└──────────────────────────────────────────────────────┘
```

### 4.2 组件与行为

| 组件 | 已授权 | 未授权 | 行为 |
| --- | --- | --- | --- |
| 应用图标与标题 | 显示 | 显示 | 使用 SupRight 应用图标，不作为操作按钮 |
| 导航侧边栏 | 显示 | 显示 | 支持未来增加设置项；当前不提供可拖拽排序 |
| 概览状态 | 显示 | 显示 | 展示已启用功能数量；完全磁盘访问实际探测成功后不显示引导 |
| 完全磁盘访问按钮 | 显示 | 显示 | 打开系统设置的对应页面；不能自动开启权限 |
| 菜单栏状态项 | 显示 | 显示 | 显示已启用功能数量；点开可打开设置或查看权限状态 |

### 4.3 菜单功能

目录状态下方显示“菜单功能”分组，所有开关默认开启：

```text
菜单功能
  新建文件
  [✓] 文本文档（.txt）     [✓] Markdown 文档（.md）
  [✓] JSON 文件（.json）   [✓] RTF 文档（.rtf）
  [✓] Word 文档（.docx）   [✓] Excel 工作簿（.xlsx）
  [✓] PowerPoint 演示文稿（.pptx） [✓] 自定义文件（输入文件名）

  辅助操作
  [✓] 复制文件名
  [✓] 复制完整路径
  [✓] 在终端中打开
```

- 开关写入 App Group，保存后不弹出确认框。
- Finder 下一次打开右键菜单时读取开关；不要求重启 Finder。
- 关闭全部新建类型时隐藏“新建文件”；关闭当前上下文所有功能时隐藏 `SupRight` 菜单。

### 4.4 文案

| 场景 | 文案 |
| --- | --- |
| 全目录模式 | `所有 Finder 目录` |
| 完全磁盘访问引导 | `请在系统设置中确认完全磁盘访问。` |
| 完全磁盘访问按钮 | `前往完全磁盘访问设置` |
| 菜单栏已启用 | `已启用 10 项功能` |
| 新建失败标题 | `无法创建文件` |
| 终端失败标题 | `无法打开终端` |

## 5. 交互细节

### 5.1 目录选择与刷新

1. 用户点击“选择工作目录…”或“更换工作目录…”。
2. 系统目录选择器返回一个目录或取消操作。
3. 选择成功后立即更新路径和状态行。
4. 主 App 提示用户重新打开 Finder 窗口；当 Finder Sync 已刷新时无需额外弹窗。
5. 选择取消时不改变原授权目录与状态。

### 5.2 错误处理

- 无法打开系统设置：显示系统可理解的错误信息，并说明可在“系统设置 > 隐私与安全性 > 完全磁盘访问”中手动操作。
- 创建模板文件失败：不创建半成品；若复制中断，删除本次新建的目标文件。
- 无法写入目录：提示“该目录不可写入”，不建议用户执行提权或关闭系统保护。
- Finder 扩展未出现：主 App 显示简短排查提示“确认 Finder 扩展已启用，并重新打开 Finder 窗口”。

## 6. 视觉规范

- 使用系统 San Francisco 字体与 macOS 默认间距；不引入自定义字体。
- 标题使用 `.title2`，正文使用 `.body`，辅助说明使用 `.footnote`。
- 主操作使用系统强调色；状态色只用于状态行：绿色表示已准备，橙色表示需处理，红色只表示失败。
- 采用 SupRight 应用图标与系统 SF Symbols 组合；图标仅用于识别和状态提示，不单独承载核心操作。
- 支持浅色和深色模式；不通过固定颜色牺牲可读性。

## 7. 无障碍与本地化

- 所有图标都有辅助功能标签；状态不得只依赖颜色表达。
- 菜单项必须可通过键盘导航和 VoiceOver 朗读。
- 中文文案控制在 Finder 菜单常见宽度内；未来本地化时保留扩展名与动作含义。

## 8. 设计确认清单

进入实现前，以本文件确认以下决定：

- 顶层 `SupRight` 子菜单 + “新建文件”二级菜单。
- MVP 只管理一个授权目录。
- 预设新建文件采用自动重名，不弹出命名对话框；“自定义文件…”要求输入完整文件名。
- Terminal.app 是 MVP 唯一的终端目标。
- DOCX、XLSX、PPTX 以 SupRight 自建空白模板创建。

确认后，技术设计应逐项映射本规格的菜单、状态、功能开关和反馈，不额外增加菜单排序或模板管理页面。
